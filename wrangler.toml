name = "drops-loop"
pages_build_output_dir = "."
compatibility_date = "2026-01-01"

# Live Loop Legends board. Create with:
#   npx wrangler kv namespace create SCORES
# then paste the id below (and bind SCORES in the Pages project settings).
[[kv_namespaces]]
binding = "SCORES"
id = "REPLACE_WITH_KV_NAMESPACE_ID"

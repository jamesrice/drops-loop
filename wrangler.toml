name = "drops-loop"
pages_build_output_dir = "."
compatibility_date = "2026-01-01"

# Live Loop Legends board. The namespace is titled LOOPDEDROP_SCORES so it
# stays distinct from drops-adventure's own "SCORES" namespace — sharing one
# would merge both games' boards. The binding stays SCORES, which is what
# functions/api/scores.js reads.
[[kv_namespaces]]
binding = "SCORES"
id = "48a2ac1087a24fbca93cefe4b08d708a"

.DS_Store
node_modules/
.wrangler/
.dev.vars

# Formiga desktop OTF source files (licensed for desktop use; only the
# converted webfont files — .woff2/.woff, licensed separately for web use —
# should ever be committed/deployed, since Cloudflare Pages serves every
# file in the repo publicly regardless of whether it's linked from HTML)
assets/fonts/*.otf
